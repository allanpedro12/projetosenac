let peso; 
peso = 90.7; 
 
if (peso <= 90.7 ) { 
    console.log( peso + ' é até  peso mosca');} 
    else {
        console.log( peso + ' não é um peso mosca ' );  
     } 

     if ( peso > 65.7 && peso <= 83.9){
        console.log( 'peso medio');
     }
     

     if ( peso >  83.9 ) { 
        console.log( ' peso pesado'); 
     }
